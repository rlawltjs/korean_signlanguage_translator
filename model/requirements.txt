# 기본 딥러닝 프레임워크 (최신 안정 버전)
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=2.2.0

# 컴퓨터 비전 및 미디어 처리
opencv-python>=4.8.0
mediapipe>=0.10.0
pillow>=9.5.0

# 데이터 처리
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scikit-learn>=1.3.0

# 유틸리티
tqdm>=4.65.0
matplotlib>=3.7.0
seaborn>=0.12.0

# 로깅 및 모니터링
tensorboard>=2.13.0

# JSON 및 파일 처리
jsonlines>=3.1.0

# 병렬 처리
joblib>=1.3.0

# YAML 설정 파일 지원
pyyaml>=6.0

# 개발 도구 (선택사항)
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0

# Intel GPU 지원 (수동 설치 필요)
# 아래 명령어로 별도 설치:
# pip install intel-extension-for-pytorch --extra-index-url https://pytorch-extension.intel.com/release-whl/stable/xpu/us/