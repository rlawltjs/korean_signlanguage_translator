# 기본 설치용 requirements (Intel GPU 없이)
# 가장 안전하고 호환성이 좋은 버전들

# PyTorch (CPU + CUDA 지원)
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=2.2.0

# 컴퓨터 비전
opencv-python==4.9.0.80
mediapipe

# 이미지 처리
pillow>=9.5.0

# 데이터 처리 (NumPy 2.0 호환성 문제로 1.x 고정)
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scikit-learn>=1.3.0

# 진행률 표시
tqdm>=4.65.0

# 시각화
matplotlib>=3.7.0
seaborn>=0.12.0

# 텐서보드
tensorboard>=2.13.0

# 설정 파일
pyyaml>=6.0

# JSON 라인 처리
jsonlines>=3.1.0

# 병렬 처리
joblib>=1.3.0